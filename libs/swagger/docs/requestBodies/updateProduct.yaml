components:
  requestBodies:
    UpdateProduct:
      description: Actualiza un producto
      content:
        multipart/form-data:
          schema:
            $ref: "#/components/schemas/Product"
